# AWS region for deploying the AMI
region = "us-east-1"

# Instance size for building the AMI
instance_size = "t2.micro"

# Base AMI to use for building (Ubuntu, for example)
base_ami = "ami-0abcdef1234567890"
